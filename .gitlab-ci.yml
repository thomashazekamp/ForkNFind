variables:
  DJANGO_SETTINGS_MODULE: "backend.settings"
  VIRTUAL_ENV: "/builds/project-0/src/backend/venv"

image: python:3.10.12

before_script:
  - |
    python -m venv venv
    source venv/bin/activate
    pip install --upgrade pip
    pip install wheel
    pip install -r src/backend/requirements.txt
    pip install -r src/sentiment_analysis/requirements.txt

test:
  script:
    - cd src/backend/
    - |
      export PYTHONPATH=src/backend:$PYTHONPATH
      python manage.py test
    - | # Run the unit tests for the sentiment analysis module
      cd ../sentiment_analysis/ 
      python3 unitTest_functionality.py